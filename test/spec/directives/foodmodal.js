'use strict';

describe('Directive: foodmodal', function () {

  // load the directive's module
  beforeEach(module('firebasepracticeApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
